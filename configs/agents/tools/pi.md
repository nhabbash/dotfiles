# Pi Appendix

Pi should consume the shared policy from `~/.config/agents/` plus this appendix.

Pi-specific notes:
- Prefer repo-local `AGENTS.md` when present.
- Keep any Pi launcher-specific prompt injection separate from the shared instruction files.
- Treat this file as the place for Pi runtime quirks only.
