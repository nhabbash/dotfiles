# Codex Appendix

Codex should consume the shared policy from `~/.config/agents/` plus this appendix.

Codex-specific notes:
- Prefer repo-local `AGENTS.md` when present.
- Keep any future Codex wrapper or launcher logic separate from the shared instruction content.
- Do not make Codex the canonical source; it should be an adapter over the shared policy.
